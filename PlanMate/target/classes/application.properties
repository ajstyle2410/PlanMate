# ----------------------------------------------------------------------------
# Primary datasource defaults (Render deployment ready)
# Values can be overridden via environment variables on Render.
# ----------------------------------------------------------------------------
spring.datasource.driver-class-name=${DATASOURCE_DRIVER:org.postgresql.Driver}
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://dpg-d3hvh7juibrs73b8lof0-a.oregon-postgres.render.com:5432/planmate?sslmode=require}
spring.datasource.username=${JDBC_DATABASE_USERNAME:root}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:zEznbKzx37qpsv2DLwFrZs9urig2IXqT}
spring.jpa.database-platform=${HIBERNATE_DIALECT:org.hibernate.dialect.PostgreSQL95Dialect}

# ----------------------------------------------------------------------------
# Optional local overrides for development convenience
# Uncomment and export SPRING_PROFILE=local to use manually if desired.
# ----------------------------------------------------------------------------
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.datasource.url=jdbc:postgresql://localhost:5432/planmet
#spring.datasource.username=postgres
#spring.datasource.password=postgres
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL95Dialect

#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.url=jdbc:mysql://localhost:3306/planmet?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=root
#spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ----------------------------------------------------------------------------
# Shared Hibernate options
# ----------------------------------------------------------------------------
hibernate.show_sql=true
hibernate.format_sql=true
hibernate.hbm2ddl.auto=update
hibernate.archive.autodetection=class,hbm
hibernate.enable_lazy_load_no_trans=true
