# ----------------------------------------------------------------------------
# PRODUCTION CONFIGURATION (Render)
# ----------------------------------------------------------------------------
# To use this profile, run with: -Dspring.profiles.active=prod
# or set SPRING_PROFILES_ACTIVE=prod environment variable

# PostgreSQL Configuration
spring.datasource.driver-class-name=org.postgresql.Driver

# Internal URL (for running ON Render)
spring.datasource.url=jdbc:postgresql://dpg-d4m2n6re5dus7382ovgg-a:5432/ajay_deshmukh

# External URL (for running locally against Prod DB - UNCOMMENT to use)
# spring.datasource.url=jdbc:postgresql://dpg-d4m2n6re5dus7382ovgg-a.oregon-postgres.render.com:5432/ajay_deshmukh

spring.datasource.username=ajay_deshmukh_user
spring.datasource.password=F75HUFKZ40T51X5cZ3c1cf53c2Ruun3A

# Hibernate Configuration for PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=update
hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Connection Pooling (HikariCP) - Optional but recommended for Prod
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.pool-name=PlanMateProdPool
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000
